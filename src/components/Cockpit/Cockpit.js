import React from "react";


const cockpit = (props) => {

    return (
        <div>
            <h1>Hi, I'm Sara Misajlovska.</h1>
            <p className="{assignedClasses.join(' ')}">This is real, this is mee</p>
            <button
                onClick={this.togglePersonsHandler}
                className={btnClass}
            >
                Toggle persons
            </button>);
        </div>
    );
};

export default cockpit;